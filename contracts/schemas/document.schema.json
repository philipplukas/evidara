{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://evidara.dev/schemas/document",
  "title": "Document",
  "description": "Canonical document entity produced by document-intelligence. Represents a single legal or regulatory document.",
  "type": "object",
  "required": [
    "document_id",
    "artifact_id",
    "source_id",
    "run_id",
    "title",
    "processed_at"
  ],
  "properties": {
    "document_id": {
      "type": "string",
      "description": "Unique identifier for the canonical document. Format: doc_{ulid}",
      "pattern": "^doc_[a-zA-Z0-9]+$"
    },
    "artifact_id": {
      "type": "string",
      "description": "ID of the raw artifact this document was derived from.",
      "pattern": "^art_[a-zA-Z0-9]+$"
    },
    "source_id": {
      "type": "string",
      "description": "ID of the source. Carried through for lineage.",
      "pattern": "^src_[a-zA-Z0-9]+$"
    },
    "source_version_id": {
      "type": "string",
      "description": "ID of the source version. Carried through for lineage.",
      "pattern": "^sv_[a-zA-Z0-9]+$"
    },
    "run_id": {
      "type": "string",
      "description": "ID of the run that produced the raw artifact.",
      "pattern": "^run_[a-zA-Z0-9]+$"
    },
    "title": {
      "type": "string",
      "description": "Title of the document."
    },
    "content": {
      "type": "string",
      "description": "Full text content of the document, if extracted."
    },
    "content_type": {
      "type": "string",
      "description": "Original content type of the source artifact."
    },
    "jurisdiction": {
      "type": "string",
      "description": "Jurisdiction the document belongs to, if assigned."
    },
    "document_type": {
      "type": "string",
      "description": "Classification of the document type (e.g., statute, regulation, decision)."
    },
    "effective_date": {
      "type": "string",
      "format": "date",
      "description": "Date the document became effective, if applicable."
    },
    "processed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the document was processed."
    },
    "processing_version": {
      "type": "string",
      "description": "Version of the processing pipeline that produced this document."
    },
    "metadata": {
      "type": "object",
      "description": "Additional processing metadata.",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
