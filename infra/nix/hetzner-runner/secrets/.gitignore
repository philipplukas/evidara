# Only commit encrypted files and templates.
# The .template is plaintext (no secrets). The .yaml is sops-encrypted (safe).
# Never commit decrypted/plaintext secret files.
*.decrypted.*
*.cleartext.*
