# Product workloads for dev (Argo CD). Placeholder ConfigMap until real manifests land.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: evidare-dev

resources:
  - evidara-gitops-placeholder.configmap.yaml
