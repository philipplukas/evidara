# Product workloads for staging (Argo CD). Placeholder ConfigMap until real manifests land.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: evidare-staging

resources:
  - evidara-gitops-placeholder.configmap.yaml
