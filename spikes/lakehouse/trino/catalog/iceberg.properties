# Trino → Nessie's Iceberg REST catalog → MinIO.
# Tables are addressed as iceberg.<namespace>.<table> — no file URIs anywhere,
# which is the whole point of ADR-0036.
connector.name=iceberg
iceberg.catalog.type=rest
iceberg.rest-catalog.uri=http://nessie:19120/iceberg/main
iceberg.rest-catalog.warehouse=warehouse

fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.path-style-access=true
s3.aws-access-key=minioadmin
s3.aws-secret-key=minioadmin
