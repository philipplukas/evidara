/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Legal Search API
 * BFF API for the Evidara legal search experience.

This API returns ViewModel shapes ready for frontend rendering.
The frontend is a pure rendering layer — all composition logic
(badges, actions, metadata, facets) lives in this API.

See ADR-0011 for contract conventions.
See ADR-0012 for layered contract governance.
See ADR-0013 for internationalization strategy.
Document body reads fall back to the Document Service
(`contracts/api/document-intelligence.openapi.yaml`) when the projection carries no body.
See ADR-0033 for the norm-hierarchy surface (`/v1/norm-hierarchy`).
See ADR-0042 for the corpus-coverage surface (`/v1/coverage`) and, in
particular, for what a coverage answer may and may not be read to mean.

 * OpenAPI spec version: 0.12.0
 */
import type { SearchRefusalViewCode } from './searchRefusalViewCode';
import type { RefusedJurisdictionView } from './refusedJurisdictionView';

/**
 * Why search declined to answer (#986).

Refusal in a legal corpus is a coverage question, not a relevance one:
"Statistikgesetz Kanton Zürich" and "Hundegesetz Kanton Bern" score
identically against a Zurich-only corpus, because the second is wrong
about the canton and not about the topic. So the check is whether the
corpus holds the jurisdiction the query names, never how well anything
scored.

 */
export interface SearchRefusalView {
  /** Machine-readable reason. `jurisdiction_not_held`: the query named a
sub-federal jurisdiction and the index holds no document for it.
 */
  code: SearchRefusalViewCode;
  /** Human- and agent-readable explanation naming the jurisdiction. */
  message: string;
  /** Every jurisdiction the query named that the corpus holds nothing
for. A query naming one held and one unheld jurisdiction is
ANSWERED, not refused, so this list is never partial.
 */
  jurisdictions: RefusedJurisdictionView[];
}
