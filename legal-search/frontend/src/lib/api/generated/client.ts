/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Legal Search API
 * BFF API for the Evidara legal search experience.

This API returns ViewModel shapes ready for frontend rendering.
The frontend is a pure rendering layer — all composition logic
(badges, actions, metadata, facets) lives in this API.

See ADR-0011 for contract conventions.
See ADR-0012 for layered contract governance.
See ADR-0013 for internationalization strategy.
Document body reads fall back to the Document Service
(`contracts/api/document-intelligence.openapi.yaml`) when the projection carries no body.
See ADR-0033 for the norm-hierarchy surface (`/v1/norm-hierarchy`).
See ADR-0042 for the corpus-coverage surface (`/v1/coverage`) and, in
particular, for what a coverage answer may and may not be read to mean.

 * OpenAPI spec version: 0.9.0
 */
import type {
  CitationGraphStats,
  CitedByResponse,
  CorpusCoverageView,
  DetailView,
  FindCitingDocumentsParams,
  FindCitingResponse,
  GetCorpusCoverageParams,
  GetDocumentSections200,
  GetNormHierarchyParams,
  IndexedProjectionDocumentPage,
  ListIndexedProjectionDocumentsParams,
  NormHierarchyView,
  ResolveCitationParams,
  ResolveCitationResponse,
  SearchContextView,
  SearchDocumentsParams,
  SearchResponseView
} from './model';

import { customFetch } from '../custom-fetch';

/**
 * Returns search results as composed ViewModels with embedded facets.
Facet counts are live (computed from the same query via OpenSearch aggs).

 * @summary Search documents
 */
export type searchDocumentsResponse200 = {
  data: SearchResponseView
  status: 200
}

export type searchDocumentsResponse400 = {
  data: void
  status: 400
}

export type searchDocumentsResponse503 = {
  data: void
  status: 503
}
    
export type searchDocumentsResponseSuccess = (searchDocumentsResponse200) & {
  headers: Headers;
};
export type searchDocumentsResponseError = (searchDocumentsResponse400 | searchDocumentsResponse503) & {
  headers: Headers;
};

export type searchDocumentsResponse = (searchDocumentsResponseSuccess | searchDocumentsResponseError)

export const getSearchDocumentsUrl = (params: SearchDocumentsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/v1/search?${stringifiedParams}` : `/v1/search`
}

export const searchDocuments = async (params: SearchDocumentsParams, options?: RequestInit): Promise<searchDocumentsResponse> => {
  
  return customFetch<searchDocumentsResponse>(getSearchDocumentsUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * Returns global search context (jurisdictions, languages, source types).
Query-independent and cacheable. Changes rarely.

 * @summary Get search context
 */
export type getSearchContextResponse200 = {
  data: SearchContextView
  status: 200
}

export type getSearchContextResponse400 = {
  data: void
  status: 400
}

export type getSearchContextResponse503 = {
  data: void
  status: 503
}
    
export type getSearchContextResponseSuccess = (getSearchContextResponse200) & {
  headers: Headers;
};
export type getSearchContextResponseError = (getSearchContextResponse400 | getSearchContextResponse503) & {
  headers: Headers;
};

export type getSearchContextResponse = (getSearchContextResponseSuccess | getSearchContextResponseError)

export const getGetSearchContextUrl = () => {


  

  return `/v1/search/context`
}

export const getSearchContext = async ( options?: RequestInit): Promise<getSearchContextResponse> => {
  
  return customFetch<getSearchContextResponse>(getGetSearchContextUrl(),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * @summary Get document detail
 */
export type getDocumentResponse200 = {
  data: DetailView
  status: 200
}

export type getDocumentResponse404 = {
  data: void
  status: 404
}
    
export type getDocumentResponseSuccess = (getDocumentResponse200) & {
  headers: Headers;
};
export type getDocumentResponseError = (getDocumentResponse404) & {
  headers: Headers;
};

export type getDocumentResponse = (getDocumentResponseSuccess | getDocumentResponseError)

export const getGetDocumentUrl = (documentId: string,) => {


  

  return `/v1/documents/${documentId}`
}

export const getDocument = async (documentId: string, options?: RequestInit): Promise<getDocumentResponse> => {
  
  return customFetch<getDocumentResponse>(getGetDocumentUrl(documentId),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * @summary Get sections of a document
 */
export type getDocumentSectionsResponse200 = {
  data: GetDocumentSections200
  status: 200
}

export type getDocumentSectionsResponse404 = {
  data: void
  status: 404
}
    
export type getDocumentSectionsResponseSuccess = (getDocumentSectionsResponse200) & {
  headers: Headers;
};
export type getDocumentSectionsResponseError = (getDocumentSectionsResponse404) & {
  headers: Headers;
};

export type getDocumentSectionsResponse = (getDocumentSectionsResponseSuccess | getDocumentSectionsResponseError)

export const getGetDocumentSectionsUrl = (documentId: string,) => {


  

  return `/v1/documents/${documentId}/sections`
}

export const getDocumentSections = async (documentId: string, options?: RequestInit): Promise<getDocumentSectionsResponse> => {
  
  return customFetch<getDocumentSectionsResponse>(getGetDocumentSectionsUrl(documentId),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * @summary Get reverse citations for a document
 */
export type getDocumentCitedByResponse200 = {
  data: CitedByResponse
  status: 200
}

export type getDocumentCitedByResponse404 = {
  data: void
  status: 404
}
    
export type getDocumentCitedByResponseSuccess = (getDocumentCitedByResponse200) & {
  headers: Headers;
};
export type getDocumentCitedByResponseError = (getDocumentCitedByResponse404) & {
  headers: Headers;
};

export type getDocumentCitedByResponse = (getDocumentCitedByResponseSuccess | getDocumentCitedByResponseError)

export const getGetDocumentCitedByUrl = (documentId: string,) => {


  

  return `/v1/documents/${documentId}/cited_by`
}

export const getDocumentCitedBy = async (documentId: string, options?: RequestInit): Promise<getDocumentCitedByResponse> => {
  
  return customFetch<getDocumentCitedByResponse>(getGetDocumentCitedByUrl(documentId),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * Returns the legal orders that bind a jurisdiction — constitutional,
federal, cantonal, municipal — ordered most authoritative first, with the
norms the corpus actually holds at each level. See ADR-0033.

This is a **traversal, not a search**. The governing chain is derived
from the jurisdiction tree
(`contracts/vocabularies/jurisdiction-hierarchy.json`), not from text
similarity: a communal ordinance is subordinate to its canton's law and
to federal law because of where the commune sits, not because the
documents resemble each other.

`coverage.missing_levels` lists the levels that bind this place but for
which the corpus holds nothing. It is load-bearing: an agent that
reasons past a missing level is fabricating, and "I do not have the
communal ordinance for this place" is a correct answer.

 * @summary What governs this place, at each level of the hierarchy of norms
 */
export type getNormHierarchyResponse200 = {
  data: NormHierarchyView
  status: 200
}

export type getNormHierarchyResponse400 = {
  data: void
  status: 400
}

export type getNormHierarchyResponse404 = {
  data: void
  status: 404
}
    
export type getNormHierarchyResponseSuccess = (getNormHierarchyResponse200) & {
  headers: Headers;
};
export type getNormHierarchyResponseError = (getNormHierarchyResponse400 | getNormHierarchyResponse404) & {
  headers: Headers;
};

export type getNormHierarchyResponse = (getNormHierarchyResponseSuccess | getNormHierarchyResponseError)

export const getGetNormHierarchyUrl = (jurisdictionId: string,
    params?: GetNormHierarchyParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/v1/norm-hierarchy/${jurisdictionId}?${stringifiedParams}` : `/v1/norm-hierarchy/${jurisdictionId}`
}

export const getNormHierarchy = async (jurisdictionId: string,
    params?: GetNormHierarchyParams, options?: RequestInit): Promise<getNormHierarchyResponse> => {
  
  return customFetch<getNormHierarchyResponse>(getGetNormHierarchyUrl(jurisdictionId,params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * Citation-graph traversal, forward direction (ADR-0033 step 3, "walk up").
Backs the `resolve_citation` MCP tool.

Accepts either a raw citation string ("SR 210") or a canonical key
("sr:210") and returns the document(s) that ARE that norm.

Resolution is DETERMINISTIC only: SR numbers, CELEX numbers, ECLIs and
Austrian BGBl references carry their identifier in the string itself.
Fuzzy citations ("Art. 36 BV", BGE references) cannot be normalized by
today's extractor and come back `resolved: false` with
`unresolved_reason: not_normalizable` — never a similarity-search guess,
which would return a plausible wrong norm.

 * @summary Resolve a citation string to the norm it points at
 */
export type resolveCitationResponse200 = {
  data: ResolveCitationResponse
  status: 200
}

export type resolveCitationResponse400 = {
  data: void
  status: 400
}
    
export type resolveCitationResponseSuccess = (resolveCitationResponse200) & {
  headers: Headers;
};
export type resolveCitationResponseError = (resolveCitationResponse400) & {
  headers: Headers;
};

export type resolveCitationResponse = (resolveCitationResponseSuccess | resolveCitationResponseError)

export const getResolveCitationUrl = (params: ResolveCitationParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/v1/citations/resolve?${stringifiedParams}` : `/v1/citations/resolve`
}

export const resolveCitation = async (params: ResolveCitationParams, options?: RequestInit): Promise<resolveCitationResponse> => {
  
  return customFetch<resolveCitationResponse>(getResolveCitationUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * Citation-graph traversal, reverse direction (ADR-0033 step 4, "walk
out"). Backs the `find_citing` MCP tool — "which decisions interpret
this provision?".

`q` is either a document id (`doc_...`) or a citation key / string
naming the norm ("sr:210", "SR 210"). A document id is first mapped to
the canonical keys that document IS.

Traversal joins on the canonical key, NOT on the `target_document_id`
denormalization written at projection time — that field is only set if
the cited document already existed when the citing document was
projected, so relying on it drops every edge whose endpoints arrived in
the wrong order.

 * @summary Find the documents that cite a norm
 */
export type findCitingDocumentsResponse200 = {
  data: FindCitingResponse
  status: 200
}

export type findCitingDocumentsResponse400 = {
  data: void
  status: 400
}
    
export type findCitingDocumentsResponseSuccess = (findCitingDocumentsResponse200) & {
  headers: Headers;
};
export type findCitingDocumentsResponseError = (findCitingDocumentsResponse400) & {
  headers: Headers;
};

export type findCitingDocumentsResponse = (findCitingDocumentsResponseSuccess | findCitingDocumentsResponseError)

export const getFindCitingDocumentsUrl = (params: FindCitingDocumentsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/v1/citations/citing?${stringifiedParams}` : `/v1/citations/citing`
}

export const findCitingDocuments = async (params: FindCitingDocumentsParams, options?: RequestInit): Promise<findCitingDocumentsResponse> => {
  
  return customFetch<findCitingDocumentsResponse>(getFindCitingDocumentsUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * What share of extracted citations actually resolve to an edge, plus the
unresolved remainder broken down by cause.

This is reported as a first-class result rather than buried in logs
because an unresolved citation is a BROKEN EDGE: consumers read a
missing edge as "no such relation exists". A graph with silently missing
edges is worse than no graph (ADR-0032), so the miss rate is part of the
contract.

 * @summary Citation graph resolution rate
 */
export type getCitationGraphStatsResponse200 = {
  data: CitationGraphStats
  status: 200
}
    
export type getCitationGraphStatsResponseSuccess = (getCitationGraphStatsResponse200) & {
  headers: Headers;
};
;

export type getCitationGraphStatsResponse = (getCitationGraphStatsResponseSuccess)

export const getGetCitationGraphStatsUrl = () => {


  

  return `/v1/citations/stats`
}

export const getCitationGraphStats = async ( options?: RequestInit): Promise<getCitationGraphStatsResponse> => {
  
  return customFetch<getCitationGraphStatsResponse>(getGetCitationGraphStatsUrl(),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * ADR-0033 §2 makes explicit coverage "the only real cure for confident
fabrication": an agent that can check whether the governing norm is in the
corpus can refuse instead of reaching for the nearest plausible text. This
endpoint is that check. See ADR-0042.

Before this path existed, the only signal about absence was
`totalResults: 0`, which conflates at least four different situations —
we do not hold the norm, we hold it but the query missed it, a filter bug
dropped it (#672 did exactly this), or the norm does not exist. An agent
cannot refuse safely on evidence that ambiguous.

## What a `not_held` answer means

**`not_held` means: the search index contains no document matching this
scope.** That is a statement about our holdings, in the first person.

It does **NOT** mean the norm does not exist, that no such law was enacted,
or that the platform has determined the law to be absent. The corpus is a
subset of the law by construction, and no endpoint over it can ever be
evidence of what the law does not contain.

The `holding` enum is therefore exactly `held | not_held`. There is no
value meaning "confirmed absent in law", and none will be added — a
requirement for one is a requirement to revisit ADR-0042.

The strongest claim this endpoint supports is a refusal: *"I do not hold
the governing norm for this question, so I will not answer it."* Per
ADR-0033 §2 that is a **correct** answer.

## What it also cannot tell you

- **Whether the corpus is current.** `last_processed_at` is when the newest
  document in the group entered the corpus — NOT evidence that the source
  was re-checked then. A jurisdiction whose law changed last week and whose
  last acquisition ran a year ago reports a year-old timestamp and no
  indication of staleness. "When did we last check?" is run history, which
  lives in platform-control (ADR-0042 §4/§5).
- **Completeness within a scope.** "We hold 41 federal acts" is not "we hold
  all federal acts". Nothing in the platform knows the denominator, so no
  percentage or completeness score is reported — inventing one is the single
  most dangerous field this endpoint could carry.
- **Anything at instrument granularity.** A group reporting `held` may still
  be missing the one act that governs the caller's question — this is the
  measured #709 failure, where the federal level was covered by the
  Bundesverfassung while TSchG/TSchV were absent. Use
  `/v1/citations/resolve` to ask about a specific norm.

 * @summary What the corpus holds for a named scope — and, explicitly, what that does not mean
 */
export type getCorpusCoverageResponse200 = {
  data: CorpusCoverageView
  status: 200
}

export type getCorpusCoverageResponse400 = {
  data: void
  status: 400
}
    
export type getCorpusCoverageResponseSuccess = (getCorpusCoverageResponse200) & {
  headers: Headers;
};
export type getCorpusCoverageResponseError = (getCorpusCoverageResponse400) & {
  headers: Headers;
};

export type getCorpusCoverageResponse = (getCorpusCoverageResponseSuccess | getCorpusCoverageResponseError)

export const getGetCorpusCoverageUrl = (params?: GetCorpusCoverageParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/v1/coverage?${stringifiedParams}` : `/v1/coverage`
}

export const getCorpusCoverage = async (params?: GetCorpusCoverageParams, options?: RequestInit): Promise<getCorpusCoverageResponse> => {
  
  return customFetch<getCorpusCoverageResponse>(getGetCorpusCoverageUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



/**
 * Index-side enumeration for the ADR-0005 reconcile diff.

ADR-0005 makes OpenSearch a serving layer only — canonical Delta is the system
of record and the index is derived from it. Keeping that true needs the diff in
BOTH directions: `document_intelligence_delta_projection_backfill` re-derives
index rows from canonical (adding what is missing), and
`document_intelligence_projection_reconcile` walks this endpoint to find indexed
documents with no canonical row behind them (removing what should not be there).
Without this endpoint nothing could list the derived side, so orphaned rows from
earlier runs stayed user-visible indefinitely.

Read-only. Rows are returned in `document_id` order and paged with an exclusive
`after` cursor, so the walk is resumable and unaffected by the deep-paging
window. `commentary_insight` rows are excluded: they are keyed by an `ins_*`
insight id and have no `published_documents` row, so a canonical diff would
classify every one of them as an orphan.

The provenance ids on each entry are the ones the originating
`document.processed` event carried; they are echoed back so a caller can build
the `document.withdrawn` event that de-indexes the row.

 * @summary Enumerate the documents currently in the search index
 */
export type listIndexedProjectionDocumentsResponse200 = {
  data: IndexedProjectionDocumentPage
  status: 200
}

export type listIndexedProjectionDocumentsResponse400 = {
  data: void
  status: 400
}
    
export type listIndexedProjectionDocumentsResponseSuccess = (listIndexedProjectionDocumentsResponse200) & {
  headers: Headers;
};
export type listIndexedProjectionDocumentsResponseError = (listIndexedProjectionDocumentsResponse400) & {
  headers: Headers;
};

export type listIndexedProjectionDocumentsResponse = (listIndexedProjectionDocumentsResponseSuccess | listIndexedProjectionDocumentsResponseError)

export const getListIndexedProjectionDocumentsUrl = (params?: ListIndexedProjectionDocumentsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/v1/projections/documents?${stringifiedParams}` : `/v1/projections/documents`
}

export const listIndexedProjectionDocuments = async (params?: ListIndexedProjectionDocumentsParams, options?: RequestInit): Promise<listIndexedProjectionDocumentsResponse> => {
  
  return customFetch<listIndexedProjectionDocumentsResponse>(getListIndexedProjectionDocumentsUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}



