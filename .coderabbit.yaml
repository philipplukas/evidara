language: "en-US"
early_access: false
reviews:
  profile: "chill"
  request_changes_workflow: false
  high_level_summary: true
  poem: false
  review_status: true
  collapse_walkthrough: false
  auto_review:
    enabled: true
    drafts: false
  path_instructions:
    - path: "legal-search/src/**"
      instructions: |
        Prioritize correctness for App Router + React 19 patterns.
        Flag URL state synchronization bugs, hydration issues, and accessibility regressions.
    - path: "legal-search/docs/**"
      instructions: |
        Check that docs stay aligned with implementation details in src/.
chat:
  auto_reply: true
