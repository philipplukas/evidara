version: 1
country_code: IT

# Canonical reference data lives in platform-control/src/platform_control/seeds/reference/
# {jurisdictions,authorities}.yaml. This file cites IDs only so the
# overlay stays in sync without duplication; the validator cross-checks
# every ID against the seed index.

jurisdiction_id: jur_it

authority_ids:
  - auth_it_gazzetta
  - auth_it_normattiva
  - auth_it_cassazione
  - auth_it_consiglio_di_stato
  - auth_it_cost
